# WEEK8 실습 기록 — 이름

> 비밀번호, `.pem` 내용, 액세스 키, Secret 값은 기록하거나 캡처하지 않는다.

## 1. 실습 환경

| 항목 | 기록 |
| --- | --- |
| 실습 날짜 | |
| AWS Region | `ap-northeast-2` |
| 운영체제·터미널 | |
| VPC ID | |
| Public Subnet ID 2개 | |
| Private DB Subnet ID 2개 | |
| ALB·Web·DB Security Group ID | |

## 2. EC2·RDS 구성

| 항목 | 기록 |
| --- | --- |
| web-1 Instance ID / Private IP | |
| web-2 Instance ID / Private IP | |
| AMI ID | |
| RDS identifier | `week8-mysql` |
| RDS endpoint | 비밀번호 제외 |
| RDS Publicly accessible | `No` 확인 여부 |
| 같은 WordPress 데이터 확인 | web-1·web-2 결과 |

## 3. ALB·장애 우회

| 시험 | 결과·증거 |
| --- | --- |
| ALB DNS에서 HTTP 200 | |
| web-1 응답 확인 | |
| web-2 응답 확인 | |
| 정상 시 HealthyHostCount | |
| web-1 Apache 중지 후 상태 | |
| 장애 중 ALB HTTP 상태 | |
| 장애 중 응답 서버 | |
| web-1 복구 후 상태 | |

### 관찰한 점

- ALB가 요청을 나누는 방식:
- Health Check가 장애를 판단하기까지 걸린 시간:
- Health Check와 실제 복구의 차이:

## 4. CloudWatch

| 지표 | 정상 구간 | 장애·요청 증가 구간에서 관찰한 변화 |
| --- | --- | --- |
| ALB `RequestCount` | | |
| `HealthyHostCount` | | |
| `UnHealthyHostCount` | | |
| `TargetResponseTime` | | |
| EC2 `CPUUtilization` | | |
| RDS `DatabaseConnections` | | |
| RDS `CPUUtilization` | | |
| RDS `FreeableMemory` | | |

### 지표로 발견한 것과 로그로 확인한 것

- 지표로 찾은 이상 시점·범위:
- 상세 원인을 확인한 위치:

## 5. API Gateway·Lambda

| 항목 | 기록 |
| --- | --- |
| API route | `GET /health` |
| HTTP status | |
| JSON의 `status`, `service` | |
| Lambda Invocations | |
| Lambda Errors | |
| CloudWatch Log Group | `/aws/lambda/week8-health` |

### EC2 방식과 비교

- 항상 실행되는 서버가 있는 경로:
- 요청 때 함수가 실행되는 경로:
- 각각 적합해 보이는 상황:

## 6. 발생한 오류와 해결

| 증상 | 확인한 위치 | 원인 | 해결 |
| --- | --- | --- | --- |
| | | | |

## 7. 비용 자원 정리 확인

- [ ] API Gateway 삭제
- [ ] Lambda 삭제
- [ ] Lambda 전용 IAM Role 삭제
- [ ] CloudWatch Dashboard·Alarm·불필요한 Log Group 삭제
- [ ] ALB 삭제
- [ ] Target Group 삭제
- [ ] EC2 두 대 Terminate
- [ ] AMI Deregister
- [ ] AMI의 EBS Snapshot 삭제
- [ ] RDS와 불필요한 Snapshot 삭제
- [ ] Security Group 삭제
- [ ] Subnet·Route Table·Internet Gateway 삭제
- [ ] VPC 삭제
- [ ] 더 사용하지 않는 Key Pair 삭제

## 8. 한 문장 정리

> 로컬에서 만든 서버 구조가 AWS에서 어떤 자원으로 나뉘었고, 장애를 어떻게 발견하고 우회했는지 한 문장으로 정리한다.
