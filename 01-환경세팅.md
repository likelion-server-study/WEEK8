# 01. 환경 세팅 — AWS 실습 시작 전 확인

이번 주차는 로컬 Docker가 아니라 실제 AWS 자원을 만듭니다. 실습 전에 계정 보안, 리전, 비용, SSH 접속 도구를 먼저 확인합니다.

## 1. 준비물

- AWS Management Console에 로그인할 수 있는 계정
- AWS 자원을 만들고 삭제할 수 있는 실습용 사용자 또는 역할
- WSL2 Ubuntu 또는 macOS 터미널
- 웹 브라우저
- 결제·비용 알림을 확인할 수 있는 계정 관리자

AWS CLI는 필수가 아닙니다. 자원 생성은 콘솔에서 하고, 터미널은 EC2 접속과 HTTP 요청 확인에 사용합니다.

## 2. 계정 보안 확인

루트 사용자는 결제·계정 관리처럼 꼭 필요한 작업에만 사용하고 MFA를 켭니다. 평소 실습은 별도의 IAM 사용자 또는 조직에서 제공한 역할로 진행합니다.

다음 정보는 화면 공유, GitHub, 메신저에 올리지 않습니다.

- AWS 비밀번호와 액세스 키
- EC2 키 페어의 `.pem` 파일
- RDS 관리자 비밀번호
- 계정 ID가 포함된 불필요한 콘솔 화면

## 3. 리전과 이름 통일

AWS 콘솔 오른쪽 위의 리전을 **서울 `ap-northeast-2`**로 맞춥니다. 이후 만드는 모든 자원도 같은 리전을 사용합니다. 리전이 다르면 EC2에서 같은 이름의 VPC나 RDS를 찾을 수 없습니다.

| 자원 | 이름 |
| --- | --- |
| VPC | `week8-vpc` |
| Public Subnet | `week8-public-a`, `week8-public-c` |
| Private DB Subnet | `week8-db-a`, `week8-db-c` |
| Internet Gateway | `week8-igw` |
| Route Table | `week8-public-rt` |
| Security Group | `week8-alb-sg`, `week8-web-sg`, `week8-db-sg` |
| EC2 | `week8-web-1`, `week8-web-2` |
| RDS | `week8-mysql` |
| Target Group | `week8-web-tg` |
| ALB | `week8-alb` |
| Lambda | `week8-health` |

모든 자원에 `Project=server-study`, `Week=8` 태그를 붙이면 마지막에 빠뜨리지 않고 찾기 쉽습니다.

## 4. 네트워크 주소 계획

이번 실습에서는 아래 주소를 그대로 사용합니다. CIDR은 네트워크가 사용할 수 있는 IP 주소 범위를 나타냅니다.

| 구역 | CIDR | 가용 영역 | 용도 |
| --- | --- | --- | --- |
| VPC 전체 | `10.8.0.0/16` | - | 8주차 전용 네트워크 |
| Public A | `10.8.1.0/24` | `ap-northeast-2a` | ALB·EC2 |
| Public C | `10.8.2.0/24` | `ap-northeast-2c` | ALB·EC2 |
| Private DB A | `10.8.11.0/24` | `ap-northeast-2a` | RDS 후보 구역 |
| Private DB C | `10.8.12.0/24` | `ap-northeast-2c` | RDS 후보 구역 |

가용 영역 문자가 계정에서 다르게 보이면 서로 다른 두 가용 영역을 선택하고 CIDR만 유지합니다.

## 5. 로컬 터미널 확인

**작업 화면: WSL2 Ubuntu 또는 macOS 터미널**

```bash
# EC2 원격 접속에 사용할 SSH 프로그램이 있는지 확인한다.
ssh -V

# 웹 서버와 ALB 응답을 확인할 curl이 있는지 확인한다.
curl --version
```

두 명령 모두 버전이 출력되면 정상입니다.

## 6. 키 페어 생성과 파일 보관

**작업 화면: EC2 콘솔 → Key pairs → Create key pair**

이름은 `week8-key`, 형식은 RSA와 `.pem`을 선택합니다. 생성할 때 내려받은 키 파일은 같은 키 페어에서 다시 다운로드할 수 없습니다.

Windows 다운로드 폴더에 받은 경우 WSL 홈의 `.ssh` 폴더로 복사해 권한을 제한합니다.

```bash
# SSH 키를 보관할 WSL 폴더를 만든다.
mkdir -p ~/.ssh

# <Windows-user>를 자신의 Windows 사용자 폴더 이름으로 바꿔 복사한다.
cp "/mnt/c/Users/<Windows-user>/Downloads/week8-key.pem" ~/.ssh/week8-key.pem

# 현재 사용자만 키 파일을 읽을 수 있게 제한한다.
chmod 400 ~/.ssh/week8-key.pem

# 파일이 존재하고 권한이 -r--------인지 확인한다.
ls -l ~/.ssh/week8-key.pem
```

키 파일은 WEEK8 저장소 안으로 복사하지 않습니다. `.pem` 파일을 Git에 추가하거나 공유하면 안 됩니다.

## 7. 현재 공인 IP 확인

Security Group의 SSH 규칙에는 인터넷 전체가 아니라 현재 접속 중인 공인 IP 하나만 허용합니다.

```bash
# 현재 인터넷 연결이 사용하는 공인 IPv4 주소를 확인한다.
curl -4 https://checkip.amazonaws.com
```

출력된 값 뒤에 `/32`를 붙인 주소를 사용합니다. 예를 들어 `203.0.113.10`이 출력되면 `203.0.113.10/32`입니다. 네트워크가 바뀌면 공인 IP도 바뀔 수 있습니다.

## 8. 비용 알림 준비

AWS Billing의 **Budgets**에서 월간 비용 예산과 이메일 알림을 설정합니다. 비용 데이터는 즉시 반영되지 않을 수 있으므로, 예산 알림은 자원 삭제를 대신하지 않습니다.

실습 중 특히 확인할 자원은 다음과 같습니다.

- 실행 중인 EC2 인스턴스와 EBS 볼륨
- RDS DB 인스턴스와 스냅샷
- Application Load Balancer
- CloudWatch의 추가 기능·장기 보관 로그
- 사용하지 않는 Elastic IP 또는 기타 네트워크 자원

## 완료 기준

- AWS 콘솔 리전이 서울 `ap-northeast-2`이다.
- 루트 계정 대신 실습용 사용자 또는 역할로 자원을 만들 수 있다.
- `ssh -V`, `curl --version`이 정상 출력된다.
- AWS 콘솔에 `week8-key` Key Pair가 있고 `.pem` 원본을 안전하게 보관했다.
- `~/.ssh/week8-key.pem`의 권한이 현재 사용자 읽기 전용이다.
- 현재 공인 IP `/32` 값을 알고 있다.
- 실습 종료 후 삭제해야 할 유료 자원을 알고 있다.

## 참고

- [AWS 계정 루트 사용자 모범 사례](https://docs.aws.amazon.com/IAM/latest/UserGuide/root-user-best-practices.html)
- [Amazon EC2 키 페어](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-key-pairs.html)
- [AWS Budgets로 비용 관리](https://docs.aws.amazon.com/cost-management/latest/userguide/budgets-managing-costs.html)
