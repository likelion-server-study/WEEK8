# 02. 이론 — 로컬 서버 구조를 AWS 자원으로 바꾸어 보기

## 목차

1. 1~7주차 구조와 AWS의 관계
2. VPC·Subnet·Route Table: 클라우드 안의 네트워크
3. Security Group: 자원 앞의 접근 허용 규칙
4. EC2·AMI·EBS: 서버, 서버 원본, 디스크
5. Apache·PHP·WordPress: 웹 서버와 웹 애플리케이션
6. RDS: 애플리케이션과 분리된 관리형 DB
7. ALB·Target Group·Health Check: 분산과 장애 우회
8. CloudWatch: AWS 자원의 지표와 로그
9. Lambda·API Gateway: 항상 켜진 서버가 없는 HTTP API
10. 이번 실습의 요청 경로

## 1. 1~7주차 구조와 AWS의 관계

앞선 주차에는 노트북 한 대 안에서 Docker와 Kubernetes로 서버 구조를 만들었습니다. AWS에서는 같은 역할을 여러 관리형 자원으로 나눕니다.

| 앞선 주차에서 다룬 것 | AWS에서 비교할 자원 | 공통점과 차이 |
| --- | --- | --- |
| 노트북·WSL에서 실행한 서버 | EC2 | 둘 다 운영체제 위에서 프로세스를 실행하지만 EC2는 AWS 데이터 센터의 가상 서버다. |
| Docker 네트워크 | VPC·Subnet | 통신 가능한 범위를 만든다는 점은 같지만 VPC는 여러 서버와 가용 영역을 포함하는 클라우드 네트워크다. |
| `ports`와 Nginx 공개 포트 | Internet Gateway·Route Table·Security Group | 인터넷 경로와 허용 포트를 각각 명시한다. |
| Docker 이미지 | AMI | 같은 실행 환경을 다시 만든다는 목적은 비슷하지만 AMI는 운영체제와 디스크 상태를 포함한 EC2 원본이다. |
| Named Volume | EBS | 실행 프로세스와 데이터를 분리해 보관한다. |
| PostgreSQL 컨테이너 | RDS for MySQL | 관계형 DB라는 역할은 같지만 RDS는 백업·패치·장애 대응 일부를 AWS가 관리한다. |
| Nginx upstream의 app1·app2 | ALB·Target Group의 EC2 두 대 | 여러 서버로 요청을 나누고 정상 서버만 사용한다. |
| Prometheus·Grafana | CloudWatch | 지표를 수집하고 그래프로 본다. CloudWatch는 AWS 자원 지표가 기본 연결되고, Prometheus는 애플리케이션 지표를 세밀하게 설계하기 좋다. |
| Kubernetes Deployment·Service | 이번 주에는 직접 이전하지 않음 | 7주차에는 로컬 클러스터의 선언·자가치유를 확인했다. AWS Kubernetes 서비스인 EKS는 이번 실습 범위에 넣지 않는다. |

## 2. VPC·Subnet·Route Table: 클라우드 안의 네트워크

### VPC

VPC는 AWS 계정 안에 만드는 논리적으로 분리된 네트워크입니다. EC2, RDS, ALB가 서로 통신하려면 같은 VPC 또는 연결된 네트워크에 있어야 합니다.

### Subnet

Subnet은 VPC의 IP 범위를 더 작은 구역으로 나눈 것입니다. 각 Subnet은 하나의 가용 영역에 속합니다.

- Public Subnet: 연결된 Route Table에 Internet Gateway로 가는 경로가 있다.
- Private Subnet: Internet Gateway로 직접 가는 경로가 없다.

`Public`과 `Private`은 이름으로 정해지지 않습니다. **Route Table의 실제 경로**로 판정합니다.

### Internet Gateway와 Route Table

Internet Gateway는 VPC와 인터넷 사이의 출입 경로입니다. Public Subnet의 Route Table에 다음 규칙을 둡니다.

```text
목적지 0.0.0.0/0 → Internet Gateway
```

`0.0.0.0/0`은 VPC 내부 경로보다 더 구체적인 규칙이 없는 모든 IPv4 목적지를 뜻합니다. Private DB Subnet의 Route Table에는 이 경로를 두지 않습니다.

이번 실습에서는 설치와 SSH를 단순하게 하기 위해 EC2를 Public Subnet에 두되, Security Group으로 직접 접근을 제한합니다. RDS는 Private DB Subnet에 두고 퍼블릭 액세스를 끕니다.

## 3. Security Group: 자원 앞의 접근 허용 규칙

Security Group은 EC2·RDS·ALB 같은 자원의 가상 방화벽입니다. 허용 규칙만 작성하고, 허용되지 않은 새 연결은 차단됩니다.

이번 실습에서는 출처를 IP 주소 대신 다른 Security Group으로 연결합니다.

```text
인터넷              → ALB SG : 80 허용
ALB SG              → Web SG : 80 허용
현재 실습자 공인 IP → Web SG : 22 허용
Web SG              → DB SG  : 3306 허용
```

DB SG의 `3306` 출처는 `0.0.0.0/0`이 아니라 **Web SG**입니다. 따라서 인터넷 사용자가 RDS 엔드포인트와 포트를 알아도 Security Group 규칙을 통과하지 못합니다.

Security Group과 Subnet은 역할이 다릅니다.

- Subnet·Route Table: 패킷이 이동할 수 있는 네트워크 경로를 정한다.
- Security Group: 그 경로가 있더라도 어떤 출처와 포트를 허용할지 정한다.

## 4. EC2·AMI·EBS: 서버, 서버 원본, 디스크

### EC2

EC2는 AWS에서 빌려 쓰는 가상 서버입니다. 운영체제 안에 Apache, PHP, WordPress 같은 소프트웨어를 설치하고 프로세스를 실행합니다.

### AMI

AMI는 새 EC2를 만들 때 사용하는 서버 원본입니다. 운영체제와 설치된 파일 상태를 바탕으로 같은 구성을 가진 서버를 다시 만들 수 있습니다. Docker 이미지와 목적은 비슷하지만 단위가 다릅니다.

- Docker 이미지: 컨테이너의 파일 시스템과 실행 환경
- AMI: EC2 운영체제와 연결된 루트 디스크의 원본

실습에서는 `week8-web-1`에 Apache·PHP·WordPress 파일을 준비한 뒤 AMI를 만들고, 그 AMI로 `week8-web-2`를 만듭니다. DB 비밀번호는 AMI에 넣기 전에 제거하거나, AMI 생성 후 각 서버에서 별도로 설정합니다.

### EBS

EBS는 EC2에 연결하는 블록 스토리지입니다. EC2 운영체제와 웹 파일이 저장됩니다. RDS 데이터는 EC2 EBS가 아니라 RDS가 관리하는 별도 스토리지에 저장됩니다.

## 5. Apache·PHP·WordPress: 웹 서버와 웹 애플리케이션

이번 실습의 EC2 내부 구성은 다음과 같습니다.

| 구성 요소 | 역할 |
| --- | --- |
| Apache HTTP Server (`httpd`) | HTTP 요청을 받고 정적 파일을 보내거나 PHP 처리를 연결하는 웹 서버 |
| PHP·PHP-FPM | WordPress의 PHP 코드를 실행하는 런타임·처리 계층 |
| WordPress | 게시물·사용자·화면 동작을 구현한 웹 애플리케이션 |
| RDS for MySQL | WordPress의 게시물과 설정을 저장하는 DB 서버 |

PetClinic은 Spring Boot와 내장 Tomcat을 사용했으므로 Tomcat이 Java 웹 요청을 처리하는 WAS 역할을 했습니다. 이번 WordPress 구성에는 별도의 Java WAS나 Tomcat이 없습니다. Apache와 PHP 실행 계층이 WordPress 요청을 처리합니다.

## 6. RDS: 애플리케이션과 분리된 관리형 DB

RDS는 MySQL, PostgreSQL 같은 관계형 DB 엔진을 AWS가 운영할 수 있게 제공하는 서비스입니다. 애플리케이션은 RDS의 **엔드포인트와 포트**로 연결합니다.

```text
WordPress → <RDS 엔드포인트>:3306 → RDS for MySQL
```

RDS를 쓴다고 SQL과 데이터 설계가 사라지는 것은 아닙니다. 테이블, 사용자 권한, 쿼리, 트랜잭션은 여전히 애플리케이션과 DB 사용자가 다룹니다. AWS는 인프라 생성, 모니터링, 백업, 패치 같은 운영 작업 일부를 관리합니다.

### DB Subnet Group

DB Subnet Group은 RDS가 어느 Subnet 후보들에 배치될 수 있는지 지정합니다. RDS용 그룹에는 서로 다른 가용 영역의 Private Subnet 두 개를 넣습니다. 단일 DB 인스턴스 실습이라도 RDS가 사용할 네트워크 후보 구역을 두 가용 영역에 걸쳐 정의합니다.

### 하나의 DB를 두 웹 서버가 공유하는 이유

`week8-web-1`과 `week8-web-2`가 각자 로컬 DB를 사용하면 어느 서버가 응답했는지에 따라 게시물이 달라집니다. 두 서버가 같은 RDS를 사용하면 ALB가 어느 서버로 요청을 보내도 같은 데이터를 조회합니다.

## 7. ALB·Target Group·Health Check: 분산과 장애 우회

### Application Load Balancer

ALB는 사용자의 HTTP 요청을 받고 규칙에 따라 정상 Target으로 전달합니다. 5주차 Nginx가 `app1`, `app2`로 요청을 보낸 것과 같은 앞단 역할입니다.

### Target Group

Target Group은 ALB가 요청을 전달할 대상 목록과 포트를 묶습니다.

```text
week8-web-tg
├─ week8-web-1:80
└─ week8-web-2:80
```

### Health Check

ALB는 각 Target에 주기적으로 HTTP 요청을 보내 정상 여부를 확인합니다. 이번 실습에서는 `/server-id.txt`가 `200`을 반환하는지 봅니다.

- 연속 성공이 기준을 넘으면 `Healthy`
- 연속 실패가 기준을 넘으면 `Unhealthy`
- ALB는 정상 Target으로 요청을 전달한다.

Health Check는 장애 원인을 고치는 기능이 아닙니다. 장애 서버를 요청 대상에서 제외해 사용자 실패를 줄이고, 운영자가 복구할 시간을 확보하는 기능입니다.

## 8. CloudWatch: AWS 자원의 지표와 로그

CloudWatch는 AWS 서비스가 내보내는 지표와 로그를 수집하고 대시보드·알람으로 보여 줍니다.

| 대상 | 이번 실습에서 볼 지표 | 알 수 있는 것 |
| --- | --- | --- |
| ALB | `RequestCount`, `TargetResponseTime` | 요청량과 대상 서버 응답 시간 |
| Target Group | `HealthyHostCount`, `UnHealthyHostCount` | 정상·비정상 웹 서버 수 |
| EC2 | `CPUUtilization`, `NetworkIn`, `NetworkOut`, `StatusCheckFailed` | 서버 부하·네트워크·인프라 상태 |
| RDS | `CPUUtilization`, `DatabaseConnections`, `FreeableMemory`, `ReadLatency`, `WriteLatency` | DB 부하·연결·메모리·I/O 지연 |
| Lambda | `Invocations`, `Errors`, `Duration` | 함수 호출 수·오류·실행 시간 |

CloudWatch 지표와 로그도 역할이 다릅니다.

- 지표: 수치의 변화를 보고 이상 발생 시점과 범위를 빠르게 찾는다.
- 로그: 특정 요청이나 오류의 상세 원인을 찾는다.

6주차의 Prometheus·Grafana는 애플리케이션 내부 지표를 직접 설계하고 조합하기 좋습니다. CloudWatch는 EC2·RDS·ALB·Lambda 같은 AWS 자원의 기본 지표가 이미 연결되어 있어 AWS 인프라를 바로 관찰하기 좋습니다. 실제 운영에서는 둘 중 하나만 고집하기보다 필요한 관찰 범위에 맞춰 함께 쓰기도 합니다.

## 9. Lambda·API Gateway: 항상 켜진 서버가 없는 HTTP API

EC2 웹 서버는 요청이 없어도 인스턴스가 실행 중입니다. Lambda는 요청이나 이벤트가 발생했을 때 함수 코드를 실행합니다.

Lambda 함수 자체에는 사용자가 호출할 일반 웹 주소가 자동으로 생기지 않습니다. 이번 실습에서는 API Gateway가 외부 HTTP 주소와 라우트를 제공하고 Lambda를 호출합니다.

```text
클라이언트
  → GET /health
API Gateway
  → Lambda 호출
Lambda
  → JSON 응답 + CloudWatch Logs 기록
```

| EC2 웹 애플리케이션 | API Gateway + Lambda |
| --- | --- |
| 운영체제와 웹 서버 프로세스를 계속 운영한다. | 서버 운영체제를 직접 관리하지 않고 함수 코드를 배포한다. |
| 장시간 실행, 기존 서버 프로그램, 세밀한 환경 제어에 적합하다. | 짧은 이벤트 처리, 간단한 API, 간헐적 작업에 적합하다. |
| 인스턴스 실행 시간과 크기를 관리한다. | 호출 수와 실행 시간 중심으로 관리한다. |

Lambda가 모든 서버를 대신하는 것은 아닙니다. 실행 시간, 상태 유지, 네트워크, 비용 구조가 다르므로 업무 특성에 맞춰 선택합니다.

## 10. 이번 실습의 요청 경로

### WordPress 요청

```text
브라우저
→ ALB:80
→ Target Group의 Healthy EC2:80
→ Apache + PHP + WordPress
→ RDS 엔드포인트:3306
→ 응답이 같은 경로로 돌아옴
```

### 서버리스 요청

```text
curl 또는 브라우저
→ API Gateway의 GET /health
→ Lambda 함수 실행
→ JSON 응답
→ 실행 기록은 CloudWatch Logs에 저장
```

두 경로를 비교하면 AWS에서도 요청의 입구, 실행 위치, 데이터 저장소, 상태 관찰 지점을 따로 설계한다는 점을 확인할 수 있습니다.

## 참고 자료

- [Internet Gateway와 Public Subnet](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Internet_Gateway.html)
- [Subnet Route Table](https://docs.aws.amazon.com/vpc/latest/userguide/subnet-route-tables.html)
- [RDS DB 인스턴스 생성과 Private 연결](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_CreateDBInstance.html)
- [ALB Target Group](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-target-groups.html)
- [ALB Health Check](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/target-group-health-checks.html)
- [Lambda 로그와 CloudWatch Logs](https://docs.aws.amazon.com/lambda/latest/dg/monitoring-cloudwatchlogs.html)

