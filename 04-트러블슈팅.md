# 04. 트러블슈팅 — AWS 8주차에서 막혔을 때

오류를 해결할 때는 요청 경로의 앞에서 뒤로 확인합니다.

```text
사용자 → ALB → EC2의 Apache·PHP·WordPress → RDS
사용자 → API Gateway → Lambda → CloudWatch Logs
```

각 단계에서 **실행 상태 → 네트워크 경로 → Security Group → 애플리케이션 로그** 순서로 좁히면 원인을 찾기 쉽습니다.

## 1. `sudo yum install mysql -y`가 `No match for argument: mysql`로 실패한다

### 원인

Amazon Linux 2023 저장소에는 `mysql`이라는 이름의 클라이언트 패키지가 없을 수 있습니다. `yum` 명령 자체의 문제가 아니라 요청한 패키지 이름이 현재 저장소와 맞지 않은 것입니다.

### 해결

```bash
# Amazon Linux 2023에서 MySQL 서버와 접속 가능한 MariaDB 호환 클라이언트를 설치한다.
sudo dnf install mariadb105 -y

# mysql 접속 명령이 생겼는지 확인한다.
mysql --version
```

RDS MySQL에 접속하기 위해 EC2에 MySQL **서버**를 따로 실행할 필요는 없습니다. EC2에는 클라이언트만 있고 실제 DB 서버는 RDS에 있습니다.

## 2. `httpd is already installed` 또는 `Nothing to do`가 나온다

오류가 아닙니다. 같은 Apache 패키지가 이미 설치되어 있어 추가 설치할 것이 없다는 뜻입니다.

```bash
# 설치 여부가 아니라 실제 실행 상태를 확인한다.
sudo systemctl is-active httpd

# 꺼져 있으면 시작과 자동 시작을 함께 설정한다.
sudo systemctl enable --now httpd
```

## 3. EC2 SSH가 `Connection timed out`으로 실패한다

다음을 순서대로 확인합니다.

1. EC2가 `Running`이고 Status checks를 통과했는가?
2. 접속 주소가 Private IP가 아니라 Public IPv4인가?
3. `week8-web-sg`가 현재 공인 IP `/32`의 TCP 22를 허용하는가?
4. EC2가 Public Subnet에 있고 해당 Route Table에 `0.0.0.0/0 → Internet Gateway`가 있는가?
5. Public Subnet에서 EC2에 Public IPv4가 할당됐는가?

현재 공인 IP가 바뀌었는지 다시 확인합니다.

```bash
curl -4 https://checkip.amazonaws.com
```

## 4. SSH가 `Permission denied (publickey)`로 실패한다

IP 경로는 열렸지만 키나 사용자 이름이 맞지 않은 경우가 많습니다.

```bash
# 키 권한을 제한한다.
chmod 400 ~/.ssh/week8-key.pem

# Amazon Linux 2023의 기본 사용자는 ec2-user다.
ssh -i ~/.ssh/week8-key.pem ec2-user@<EC2_PUBLIC_IP>
```

인스턴스를 만들 때 선택한 Key Pair와 현재 `.pem` 파일이 같은지 확인합니다.

## 5. EC2 Public IP의 웹 페이지가 열리지 않는다

ALB 생성 전 직접 확인 단계라면 다음을 봅니다.

```bash
# EC2 안에서 Apache가 실행 중인지 확인한다.
sudo systemctl is-active httpd

# EC2 안에서 로컬 HTTP가 200인지 확인한다.
curl -i http://localhost/server-id.txt

# 최근 Apache 오류를 확인한다.
sudo journalctl -u httpd --since '10 minutes ago' --no-pager
sudo tail -n 50 /var/log/httpd/error_log
```

EC2 내부 `localhost`는 성공하지만 Public IP만 실패하면 Apache보다 Subnet 경로와 `week8-web-sg`의 임시 HTTP 80 규칙을 확인합니다.

7-4에서 직접 HTTP 규칙을 지운 뒤에는 Public IP 접속이 실패하는 것이 정상입니다. 그때는 ALB DNS로 접속합니다.

## 6. EC2에서 RDS 연결이 시간 초과된다

```bash
# DNS가 RDS 엔드포인트를 IP로 해석하는지 확인한다.
getent hosts <RDS_ENDPOINT>

# MySQL 클라이언트로 실제 연결을 시도한다.
mysql -h <RDS_ENDPOINT> -P 3306 -u admin -p
```

시간 초과라면 다음을 확인합니다.

- EC2와 RDS가 같은 `week8-vpc`에 있는가?
- RDS 상태가 `Available`인가?
- `week8-db-sg` 인바운드 `3306`의 Source가 **week8-web-sg**인가?
- RDS에 실제로 `week8-db-sg`가 연결되어 있는가?
- 엔드포인트를 복사할 때 `https://`나 공백을 붙이지 않았는가?

RDS가 Private인 것은 정상입니다. 연결 시험은 인터넷의 로컬 PC가 아니라 `week8-web-sg`가 연결된 EC2에서 합니다.

## 7. RDS가 `Access denied for user`를 반환한다

네트워크 연결은 성공했지만 사용자·비밀번호·권한이 맞지 않은 상태입니다.

- 관리자 이름이 실제 RDS 생성 때 사용한 값인지 확인합니다.
- `-p` 뒤에 비밀번호를 명령문으로 붙이지 말고 프롬프트에서 입력합니다.
- WordPress 계정의 DB 권한을 관리자 연결에서 다시 확인합니다.

```sql
SELECT user, host FROM mysql.user WHERE user = 'wpuser';
SHOW GRANTS FOR 'wpuser'@'%';
```

비밀번호를 잊었다면 채팅이나 GitHub에서 찾지 말고 RDS 또는 DB 계정의 비밀번호를 안전하게 재설정합니다.

## 8. WordPress에 `Error establishing a database connection`이 나온다

두 EC2에서 다음 네 값이 모두 같은지 확인합니다.

```bash
# 비밀번호 자체를 출력하지 않고 DB 관련 줄의 위치와 키 이름을 확인한다.
sudo grep -nE "DB_NAME|DB_USER|DB_HOST" /var/www/html/wp-config.php
```

- `DB_NAME`: `wordpress`
- `DB_USER`: `wpuser`
- `DB_PASSWORD`: 6-1에서 만든 실제 비밀번호
- `DB_HOST`: 실제 RDS Endpoint와 `:3306`

그 다음 WordPress 계정으로 직접 접속합니다.

```bash
mysql -h <RDS_ENDPOINT> -P 3306 -u wpuser -p wordpress
```

직접 접속은 성공하지만 WordPress만 실패하면 PHP 모듈과 로그를 확인합니다.

```bash
php -m | grep -E 'mysqli|pdo_mysql'
sudo tail -n 100 /var/log/httpd/error_log
sudo journalctl -u php-fpm --since '10 minutes ago' --no-pager
```

## 9. web-1과 web-2에서 서로 다른 WordPress 화면이 보인다

두 서버가 서로 다른 DB를 가리키거나 한쪽 설정이 끝나지 않은 경우가 많습니다.

```bash
# 각 EC2에서 DB 호스트와 DB 이름을 비교한다. 비밀번호 줄은 출력하지 않는다.
sudo grep -nE "DB_NAME|DB_USER|DB_HOST" /var/www/html/wp-config.php
```

두 서버가 같은 RDS 엔드포인트와 `wordpress` DB를 사용해야 합니다.

게시물은 같지만 업로드 이미지가 한쪽에서만 보인다면 DB 문제가 아닙니다. 기본 WordPress 업로드 파일은 각 EC2의 로컬 EBS에 따로 저장됩니다. 이번 실습은 파일 업로드를 공유하지 않습니다.

## 10. Target Group이 계속 `Unhealthy`다

Target 상태 상세의 Reason code를 먼저 봅니다.

| 상태 상세 | 주로 확인할 것 |
| --- | --- |
| `Target.Timeout` | web SG가 ALB SG의 80을 허용하는지, Apache 실행 여부 |
| `Target.ResponseCodeMismatch` | `/server-id.txt`가 실제로 200인지, Success code 설정 |
| `Target.FailedHealthChecks` | 포트 80, Apache 로그, 파일 권한 |

각 EC2 내부에서 확인합니다.

```bash
sudo systemctl is-active httpd
curl -i http://localhost/server-id.txt
sudo tail -n 50 /var/log/httpd/access_log
sudo tail -n 50 /var/log/httpd/error_log
```

Target Group 포트가 80이고, `week8-web-sg`가 **Source: week8-alb-sg, Port: 80**을 허용해야 합니다.

## 11. ALB 주소가 `503 Service Unavailable`을 반환한다

ALB Listener와 Target Group 연결은 존재하지만 요청을 보낼 Healthy Target이 없는 경우가 많습니다.

1. Target Group에 두 EC2가 등록됐는지 확인합니다.
2. 상태가 `Healthy`인지 확인합니다.
3. ALB Listener HTTP 80의 Default action이 `week8-web-tg`인지 확인합니다.
4. ALB가 Public Subnet 두 개와 `week8-alb-sg`를 사용하는지 확인합니다.

모든 Target이 Unhealthy일 때는 ALB가 정상 장애 우회를 할 대상이 없습니다. 먼저 Apache나 Health Check를 복구합니다.

## 12. 반복 호출에도 한 EC2 이름만 나온다

짧은 요청 묶음이 정확히 반반 나뉘어야 하는 것은 아닙니다. 다음을 확인합니다.

- Target Group에서 두 Target이 모두 `Healthy`인가?
- 요청 주소가 ALB DNS인가, EC2 Public IP가 아닌가?
- `/server-id.txt`가 두 EC2에서 서로 다른 내용을 갖는가?

```bash
# 요청마다 쿼리 값을 바꿔 30번 확인한다.
i=1
while [ "$i" -le 30 ]; do
  curl -s "http://$ALB_DNS/server-id.txt?check=$i"
  i=$((i + 1))
done
```

## 13. CloudWatch 그래프에 방금 요청이 보이지 않는다

기본 지표는 실시간 터미널 로그처럼 즉시 나타나지 않습니다.

- 콘솔 리전이 서울인지 확인합니다.
- 그래프 시간 범위를 `Last 1 hour`로 넓힙니다.
- Period를 1분 또는 5분으로 맞춥니다.
- 올바른 ALB, Target Group, DB instance 차원을 선택했는지 확인합니다.
- 요청 생성 후 몇 분 기다립니다.

Apache URL별 로그는 EC2 기본 CloudWatch 지표에 자동 포함되지 않습니다. 이번 실습에서는 `/var/log/httpd/access_log`에서 보고, 중앙 로그 수집은 별도 CloudWatch Agent 설정이 필요합니다.

## 14. Lambda Test는 되는데 API Gateway가 404를 반환한다

- API 유형이 HTTP API인지 확인합니다.
- Route가 정확히 `GET /health`인지 확인합니다.
- Invoke URL 뒤에 `/health`를 한 번만 붙였는지 확인합니다.
- `$default` Stage의 자동 배포가 켜져 있는지 확인합니다.
- Route의 Integration이 `week8-health`인지 확인합니다.

```bash
curl -i "$API_URL/health"
```

`POST /health`, `GET /`, 잘못된 Stage 경로는 현재 Route와 다르므로 404가 날 수 있습니다.

## 15. API Gateway가 500 또는 502를 반환한다

Lambda 코드 오류나 응답 형식 문제를 확인합니다.

1. Lambda의 Monitor → View CloudWatch logs를 엽니다.
2. `/aws/lambda/week8-health`의 최신 Log stream을 봅니다.
3. 함수 코드가 `statusCode`와 문자열 `body`를 반환하는지 확인합니다.
4. 수정 후 **Deploy**를 눌렀는지 확인합니다.

Lambda Test도 실패하면 API Gateway보다 Lambda 코드를 먼저 고칩니다.

## 16. Security Group 또는 VPC가 삭제되지 않는다

다른 자원이 아직 참조하고 있기 때문입니다.

- ALB가 완전히 삭제됐는가?
- Target Group이 남아 있는가?
- EC2가 `Terminated` 상태가 되었는가?
- RDS와 연결된 네트워크 인터페이스가 사라졌는가?
- Security Group이 다른 Security Group 규칙의 Source로 남아 있는가?
- Internet Gateway가 VPC에 연결된 상태인가?

ALB와 RDS 삭제는 시간이 걸립니다. 강제로 순서를 건너뛰지 말고 [03-실습.md의 삭제 순서](03-실습.md#11-유료-자원-정리)를 따릅니다.

## 17. 실습 후에도 비용이 보인다

비용 화면은 지연되어 반영될 수 있고, 실행 자원을 삭제해도 저장 자원이 남을 수 있습니다.

다음을 다시 확인합니다.

- EC2 인스턴스와 별도 EBS 볼륨
- AMI 등록 해제 후 남은 EBS Snapshot
- RDS DB와 Final·Manual Snapshot
- Application Load Balancer
- CloudWatch Dashboard·Alarm·장기 보관 Log Group
- Elastic IP 등 추가 생성한 자원

Budget 알림은 비용을 차단하는 기능이 아닙니다. 콘솔에서 자원 삭제 여부를 직접 확인해야 합니다.

## 공식 참고 자료

- [Amazon Linux 2023 WordPress 패키지](https://docs.aws.amazon.com/linux/al2023/ug/hosting-wordpress-aml-2023.html)
- [ALB Target 상태 확인과 사유 코드](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/check-target-health.html)
- [RDS DB 인스턴스 생성과 연결](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_CreateDBInstance.html)
- [Lambda 로그를 CloudWatch Logs에서 보기](https://docs.aws.amazon.com/lambda/latest/dg/monitoring-cloudwatchlogs.html)

