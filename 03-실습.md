# 03. 실습 — EC2·RDS·ALB를 구성하고 CloudWatch로 관찰하기

## 시작 전에 — 오늘 만드는 것

이번 실습은 세 덩어리로 진행합니다.

1. VPC 안에 EC2 웹 서버 두 대와 Private RDS를 구성한다.
2. ALB가 두 웹 서버로 요청을 나누고 장애 서버를 제외하는지 확인한다.
3. CloudWatch로 AWS 자원을 관찰하고, API Gateway·Lambda의 서버리스 요청 경로를 비교한다.

**AWS 콘솔 리전은 모든 단계에서 서울 `ap-northeast-2`로 유지합니다.** 이름과 CIDR은 [01-환경세팅.md](01-환경세팅.md)의 표를 그대로 사용합니다.

## 0. 실습 기록 준비

**이 단계에서 확인하는 것:** 만든 자원과 삭제할 자원을 빠뜨리지 않도록 기록 파일을 먼저 준비합니다.

`records/_템플릿.md`를 복사해 자신의 기록 파일을 만듭니다. AWS 키, DB 비밀번호, 계정 ID는 기록하지 않습니다.

기록할 값은 다음과 같습니다.

- VPC·Subnet·Security Group ID
- EC2 인스턴스 ID와 Private IP
- RDS 엔드포인트(비밀번호 제외)
- ALB DNS 이름
- Target Group의 Healthy·Unhealthy 결과
- CloudWatch 화면에서 확인한 지표
- API Gateway 호출 URL과 HTTP 상태
- 삭제 완료 여부

---

## 1. VPC와 Public·Private Subnet 만들기

**이 단계에서 확인하는 것:** 인터넷 요청을 받는 구역과 DB를 둘 내부 구역을 서로 다른 경로로 나눕니다.

### 1-1. VPC 생성

**작업 화면: AWS 콘솔 → VPC → Your VPCs → Create VPC**

| 항목 | 입력 값 |
| --- | --- |
| Resources to create | VPC only |
| Name tag | `week8-vpc` |
| IPv4 CIDR | `10.8.0.0/16` |
| IPv6 | 없음 |
| Tenancy | Default |

생성 후 **DNS hostnames**와 **DNS resolution**이 활성화되어 있는지 확인합니다.

### 1-2. Subnet 네 개 생성

**작업 화면: VPC → Subnets → Create subnet**

| 이름 | 가용 영역 | IPv4 CIDR | 역할 |
| --- | --- | --- | --- |
| `week8-public-a` | `ap-northeast-2a` | `10.8.1.0/24` | ALB·web-1 |
| `week8-public-c` | `ap-northeast-2c` | `10.8.2.0/24` | ALB·web-2 |
| `week8-db-a` | `ap-northeast-2a` | `10.8.11.0/24` | RDS 후보 구역 |
| `week8-db-c` | `ap-northeast-2c` | `10.8.12.0/24` | RDS 후보 구역 |

계정에서 가용 영역 문자가 다르게 보이면 서로 다른 두 영역을 선택합니다.

Public Subnet 두 개는 **Edit subnet settings → Enable auto-assign public IPv4 address**를 켭니다. Private DB Subnet에는 켜지 않습니다.

### 1-3. Internet Gateway와 Public Route Table 구성

**작업 화면: VPC → Internet gateways**

1. `week8-igw`를 생성합니다.
2. `week8-vpc`에 연결합니다.

**작업 화면: VPC → Route tables**

1. `week8-public-rt`를 `week8-vpc`에 생성합니다.
2. Routes에 `0.0.0.0/0 → week8-igw`를 추가합니다.
3. Subnet associations에 `week8-public-a`, `week8-public-c`만 연결합니다.

### 확인해야 할 결과

| 확인 위치 | 정상 결과 |
| --- | --- |
| `week8-public-rt`의 Routes | `10.8.0.0/16 → local`, `0.0.0.0/0 → igw-...` |
| Public Subnet association | `week8-public-a`, `week8-public-c` |
| DB Subnet의 Route Table | Internet Gateway로 가는 `0.0.0.0/0` 경로 없음 |

Public·Private라는 이름만으로 공개 여부가 정해지는 것이 아닙니다. Public Subnet만 Internet Gateway 경로를 갖고, DB Subnet은 VPC 내부 경로만 갖도록 만든 것이 핵심입니다.

---

## 2. Security Group 세 개 만들기

**이 단계에서 확인하는 것:** 사용자는 ALB로만 접속하고, 웹 서버와 DB는 바로 앞 계층에서 오는 연결만 받게 합니다.

**작업 화면: EC2 콘솔 → Security Groups → Create security group**

세 Security Group 모두 `week8-vpc`에 만듭니다.

### `week8-alb-sg`

| Type | Port | Source | 의미 |
| --- | --- | --- | --- |
| HTTP | 80 | `0.0.0.0/0` | 인터넷 사용자가 ALB에 접속 |

### `week8-web-sg`

| Type | Port | Source | 의미 |
| --- | --- | --- | --- |
| SSH | 22 | 현재 공인 IP `/32` | 실습자만 EC2 관리 접속 |
| HTTP | 80 | 현재 공인 IP `/32` | ALB 생성 전 직접 확인용 임시 규칙 |
| HTTP | 80 | `week8-alb-sg` | ALB가 EC2 웹 서버에 요청 전달 |

### `week8-db-sg`

| Type | Port | Source | 의미 |
| --- | --- | --- | --- |
| MySQL/Aurora | 3306 | `week8-web-sg` | 웹 서버만 RDS MySQL 접속 |

모든 Security Group의 기본 Outbound 규칙은 이번 실습에서 그대로 둡니다.

### 확인해야 할 결과

- `week8-db-sg`에 `0.0.0.0/0` 출처의 `3306` 규칙이 없어야 합니다.
- `week8-web-sg`의 SSH 출처가 `0.0.0.0/0`이 아니라 현재 공인 IP `/32`여야 합니다.
- Security Group 이름이 아니라 **ID와 연결 대상**도 `week8-vpc`인지 확인합니다.

Security Group을 계층별로 나누면 ALB 주소는 공개하면서도 EC2의 HTTP와 RDS의 MySQL을 필요한 앞 계층에만 허용할 수 있습니다.

---

## 3. Private RDS for MySQL 만들기

**이 단계에서 확인하는 것:** DB는 인터넷에 공개하지 않고, 두 웹 서버가 함께 사용할 MySQL을 Private Subnet에 배치합니다.

### 3-1. DB Subnet Group 생성

**작업 화면: RDS 콘솔 → Subnet groups → Create DB subnet group**

| 항목 | 입력 값 |
| --- | --- |
| Name | `week8-db-subnet-group` |
| VPC | `week8-vpc` |
| Availability Zones | 앞에서 선택한 서로 다른 두 영역 |
| Subnets | `week8-db-a`, `week8-db-c` |

### 3-2. RDS 생성

**작업 화면: RDS → Databases → Create database**

콘솔의 세부 이름은 시점에 따라 조금 달라질 수 있습니다. 다음 조건은 반드시 맞춥니다.

| 항목 | 선택 값 |
| --- | --- |
| Creation method | Standard create |
| Engine | MySQL |
| Template | Free tier가 보이면 Free tier, 아니면 Dev/Test의 가장 작은 실습용 구성 |
| DB instance identifier | `week8-mysql` |
| Master username | `admin` |
| Master password | 직접 만든 강한 비밀번호 — 문서에 기록하지 않음 |
| Availability | Single DB instance / Single-AZ |
| Storage | 실습에 필요한 최소 범위, Storage autoscaling 비활성화 |
| VPC | `week8-vpc` |
| DB subnet group | `week8-db-subnet-group` |
| Public access | **No** |
| VPC Security Group | 기존 `week8-db-sg` |
| Initial database name | `wordpress` |
| Deletion protection | 실습 정리를 위해 비활성화 |

`Available`이 될 때까지 기다린 뒤 Connectivity & security에서 **Endpoint**와 **Port 3306**을 기록합니다.

### 확인해야 할 결과

| 확인 항목 | 정상 결과 |
| --- | --- |
| Status | `Available` |
| Publicly accessible | `No` |
| VPC | `week8-vpc` |
| Security Group | `week8-db-sg` |
| Port | `3306` |

RDS 엔드포인트가 보인다는 사실은 인터넷에서 접속할 수 있다는 뜻이 아닙니다. Private Subnet의 경로, `Public access: No`, DB Security Group 규칙을 함께 통과해야 연결됩니다.

---

## 4. 첫 번째 EC2에 웹 실행 환경 준비하기

**이 단계에서 확인하는 것:** Amazon Linux 2023 EC2에 Apache·PHP·WordPress 파일과 MySQL 접속 도구를 준비합니다.

### 4-1. `week8-web-1` 생성

**작업 화면: EC2 → Instances → Launch instance**

| 항목 | 선택 값 |
| --- | --- |
| Name | `week8-web-1` |
| AMI | Amazon Linux 2023 |
| Instance type | 계정의 무료·실습 범위에서 선택 |
| Key pair | 01-환경세팅에서 준비한 키 |
| VPC | `week8-vpc` |
| Subnet | `week8-public-a` |
| Auto-assign public IP | Enable |
| Security Group | 기존 `week8-web-sg` |
| Storage | 기본 루트 볼륨 |

인스턴스가 `Running`이고 Status checks가 통과하면 Public IPv4를 기록합니다.

### 4-2. SSH 접속

**작업 화면: 로컬 WSL2 Ubuntu 또는 macOS 터미널**

```bash
# EC2 화면의 실제 Public IPv4를 따옴표 안에 붙여 넣는다.
WEB1_PUBLIC_IP='여기에 web-1 Public IPv4 입력'

# 위 주소의 Amazon Linux EC2에 접속한다.
ssh -i ~/.ssh/week8-key.pem "ec2-user@$WEB1_PUBLIC_IP"
```

처음 접속할 때 서버 지문을 신뢰할지 묻는 메시지가 나오면 IP가 생성한 EC2와 같은지 확인한 뒤 `yes`를 입력합니다. 프롬프트가 `[ec2-user@ip-... ~]$`로 바뀌면 EC2 안에 접속한 상태입니다.

### 4-3. 패키지 설치와 서비스 시작

**작업 화면: `week8-web-1` SSH 터미널**

```bash
# Amazon Linux 패키지 목록과 설치 패키지를 최신 상태로 맞춘다.
sudo dnf update -y

# Apache, PHP 실행 환경, WordPress 다운로드 도구, MySQL 호환 클라이언트를 설치한다.
sudo dnf install -y httpd php php-fpm php-mysqlnd php-mysqli php-json php-gd php-mbstring wget tar mariadb105

# Apache와 PHP-FPM을 지금 시작하고 EC2 재부팅 뒤에도 자동 시작하게 한다.
sudo systemctl enable --now httpd php-fpm

# 두 서비스가 실행 중인지 확인한다.
sudo systemctl is-active httpd php-fpm

# RDS MySQL에 접속할 명령이 설치됐는지 확인한다.
mysql --version
```

`active`가 두 줄, MySQL 호환 클라이언트 버전이 한 줄 출력되면 정상입니다. `sudo yum install mysql -y`는 Amazon Linux 2023에서 패키지를 찾지 못할 수 있으므로 사용하지 않습니다.

### 4-4. WordPress 파일 준비

```bash
# 임시 폴더로 이동해 공식 WordPress 압축 파일을 내려받고 푼다.
cd /tmp
wget https://wordpress.org/latest.tar.gz
tar -xzf latest.tar.gz

# WordPress 파일을 Apache의 문서 루트로 복사한다.
sudo cp -a /tmp/wordpress/. /var/www/html/

# Apache가 파일을 읽고 필요한 폴더에 쓸 수 있도록 소유자와 권한을 정리한다.
sudo chown -R apache:apache /var/www/html
sudo find /var/www/html -type d -exec chmod 755 {} \;
sudo find /var/www/html -type f -exec chmod 644 {} \;

# WordPress의 핵심 파일이 복사됐는지 확인한다.
ls -l /var/www/html/index.php /var/www/html/wp-config-sample.php
```

### 명령 해설

| 명령 | 역할 |
| --- | --- |
| `dnf install` | Amazon Linux 2023 저장소에서 패키지를 설치한다. |
| `systemctl enable --now` | 서비스를 지금 시작하고 재부팅 후 자동 시작도 설정한다. |
| `wget` | URL의 파일을 현재 폴더에 내려받는다. |
| `tar -xzf` | gzip으로 압축된 tar 파일을 푼다. |
| `cp -a source/. destination/` | 하위 파일과 속성을 유지하며 내용 전체를 복사한다. |
| `chown -R apache:apache` | 파일 소유 사용자·그룹을 Apache로 바꾼다. |
| `find ... -exec chmod` | 폴더는 755, 파일은 644로 각각 권한을 적용한다. |

이 단계에서는 아직 DB 접속 정보를 넣지 않습니다. DB 비밀번호가 포함되지 않은 서버 원본을 먼저 만든 뒤 두 EC2에 각각 설정합니다.

---

## 5. AMI로 두 번째 웹 서버 만들기

**이 단계에서 확인하는 것:** 한 서버에 설치한 공통 환경을 AMI로 복제해 같은 웹 서버를 두 대 만듭니다.

### 5-1. AMI 생성

`week8-web-1`의 설치가 끝났으면 EC2 콘솔에서 다음을 실행합니다.

**작업 화면: EC2 → Instances → `week8-web-1` 선택 → Actions → Image and templates → Create image**

| 항목 | 값 |
| --- | --- |
| Image name | `week8-web-ami` |
| Reboot | 기본값 유지 |

EC2 → AMIs에서 상태가 `Available`이 될 때까지 기다립니다.

### 5-2. AMI로 `week8-web-2` 생성

**작업 화면: EC2 → AMIs → `week8-web-ami` → Launch instance from AMI**

| 항목 | 값 |
| --- | --- |
| Name | `week8-web-2` |
| Subnet | `week8-public-c` |
| Auto-assign public IP | Enable |
| Security Group | 기존 `week8-web-sg` |
| Key pair | web-1과 같은 실습 키 |

나머지는 web-1과 같은 크기와 설정을 사용합니다.

### 5-3. 서버 식별 파일 만들기

두 EC2에 각각 SSH로 접속해 서로 다른 내용을 기록합니다.

**`week8-web-1` SSH 터미널**

```bash
# ALB가 어느 EC2의 응답을 반환했는지 구분할 정적 파일을 만든다.
echo 'week8-web-1' | sudo tee /var/www/html/server-id.txt
```

**`week8-web-2` SSH 터미널**

```bash
# 두 번째 EC2에는 다른 식별 값을 기록한다.
echo 'week8-web-2' | sudo tee /var/www/html/server-id.txt
```

각 서버에서 다음을 실행합니다.

```bash
# Apache가 식별 파일을 200으로 반환하는지 EC2 내부에서 확인한다.
curl -i http://localhost/server-id.txt
```

`HTTP/1.1 200 OK`와 해당 서버 이름이 출력되어야 합니다.

AMI는 공통 설치를 반복하지 않게 해 주지만, 서버별 이름·IP·비밀번호까지 자동으로 올바르게 바꾸지는 않습니다. 복제 뒤 달라져야 하는 값은 별도로 설정해야 합니다.

---

## 6. 두 웹 서버를 같은 RDS에 연결하기

**이 단계에서 확인하는 것:** web-1·web-2가 같은 RDS의 WordPress 데이터를 읽고 쓰게 합니다.

### 6-1. EC2에서 RDS 연결 확인과 앱 사용자 생성

**작업 화면: `week8-web-1` SSH 터미널**

```bash
# RDS 화면의 실제 Endpoint를 따옴표 안에 붙여 넣는다. https://와 :3306은 넣지 않는다.
RDS_ENDPOINT='여기에 RDS Endpoint 입력'

# 관리자 계정으로 RDS에 접속한다. -p 뒤에는 비밀번호를 붙이지 않는다.
mysql -h "$RDS_ENDPOINT" -P 3306 -u admin -p
```

RDS 관리자 비밀번호를 입력하면 `mysql>` 프롬프트가 나타납니다. 다음 SQL에서 `<WP_DB_PASSWORD>`는 새로 만든 강한 비밀번호로 바꿉니다.

```sql
-- WordPress만 사용할 별도 DB 계정을 만든다.
CREATE USER 'wpuser'@'%' IDENTIFIED BY '<WP_DB_PASSWORD>';

-- wordpress DB에 필요한 권한만 준다.
GRANT ALL PRIVILEGES ON wordpress.* TO 'wpuser'@'%';
FLUSH PRIVILEGES;

-- 초기 DB와 계정이 보이는지 확인한다.
SHOW DATABASES;
SELECT user, host FROM mysql.user WHERE user = 'wpuser';

EXIT;
```

`wordpress` DB와 `wpuser`가 출력되면 정상입니다.

### 6-2. 두 EC2의 WordPress 설정

web-1과 web-2에서 각각 같은 작업을 실행합니다.

```bash
# 샘플 파일을 실제 WordPress 설정 파일로 복사한다.
sudo cp /var/www/html/wp-config-sample.php /var/www/html/wp-config.php

# DB 이름·사용자·비밀번호·RDS 엔드포인트를 수정한다.
sudo nano /var/www/html/wp-config.php
```

`nano`에서 다음 네 줄을 찾아 실제 값으로 바꿉니다.

```php
define( 'DB_NAME', 'wordpress' );
define( 'DB_USER', 'wpuser' );
define( 'DB_PASSWORD', '<WP_DB_PASSWORD>' );
define( 'DB_HOST', '<RDS_ENDPOINT>:3306' );
```

인증 키·salt는 **web-1에서 한 번만** 생성합니다.

```bash
# WordPress 공식 API에서 현재 사이트용 무작위 인증 키와 salt를 한 번 만든다.
curl -s https://api.wordpress.org/secret-key/1.1/salt/
```

출력된 `AUTH_KEY`부터 `NONCE_SALT`까지 여덟 줄을 **두 EC2의 `wp-config.php`에 똑같이** 넣습니다. 서버마다 다른 salt를 넣으면 ALB가 요청을 다른 서버로 보낼 때 WordPress 로그인 쿠키 검증이 달라질 수 있습니다. 저장은 `Ctrl+O` → Enter, 종료는 `Ctrl+X`입니다.

```bash
# 설정 파일은 Apache만 읽을 수 있도록 소유권과 권한을 제한한다.
sudo chown apache:apache /var/www/html/wp-config.php
sudo chmod 640 /var/www/html/wp-config.php

# 설정을 읽도록 서비스를 재시작한다.
sudo systemctl restart php-fpm httpd

# Apache가 실행 중인지 확인한다.
sudo systemctl is-active php-fpm httpd
```

### 6-3. 두 서버의 WordPress 준비 상태 확인

ALB를 만들기 전에는 `week8-web-sg`에 현재 IP의 HTTP 규칙이 있으므로 두 주소의 응답을 확인할 수 있습니다.

```text
http://<WEB1_PUBLIC_IP>/wp-admin/install.php
http://<WEB2_PUBLIC_IP>/wp-admin/install.php
```

두 주소 모두 DB 연결 오류가 아니라 WordPress 설치 화면을 반환하면 준비가 끝난 것입니다. **여기서는 설치 완료 버튼을 누르지 않습니다.** Public IP에서 설치를 끝내면 WordPress의 대표 주소가 특정 EC2 Public IP로 저장될 수 있습니다. 7단계에서 ALB를 만든 뒤 ALB DNS로 한 번만 초기 설정합니다.

> 게시물 데이터는 RDS를 공유하지만, 사용자가 업로드한 파일은 기본적으로 각 EC2의 `/var/www/html/wp-content/uploads`에 따로 저장됩니다. 이번 실습에서는 업로드 기능을 사용하지 않습니다. 운영 환경에서는 S3나 EFS 같은 공유 저장 방식을 별도로 설계해야 합니다.

---

## 7. ALB로 두 웹 서버 연결하기

**이 단계에서 확인하는 것:** 사용자는 ALB 주소 하나로 접속하고 ALB가 정상 EC2 두 대에 요청을 나누게 합니다.

### 7-1. Target Group 생성

**작업 화면: EC2 → Target Groups → Create target group**

| 항목 | 값 |
| --- | --- |
| Target type | Instances |
| Name | `week8-web-tg` |
| Protocol / Port | HTTP / 80 |
| VPC | `week8-vpc` |
| Health check protocol | HTTP |
| Health check path | `/server-id.txt` |
| Success code | `200` |

다음 화면에서 `week8-web-1`, `week8-web-2`를 선택해 Port 80으로 등록합니다.

### 7-2. Application Load Balancer 생성

**작업 화면: EC2 → Load Balancers → Create load balancer → Application Load Balancer**

| 항목 | 값 |
| --- | --- |
| Name | `week8-alb` |
| Scheme | Internet-facing |
| IP address type | IPv4 |
| VPC | `week8-vpc` |
| Mappings | `week8-public-a`, `week8-public-c` |
| Security Group | `week8-alb-sg` |
| Listener | HTTP 80 |
| Default action | Forward to `week8-web-tg` |

생성 후 Target Group의 Targets 탭에서 두 인스턴스가 모두 `Healthy`가 될 때까지 기다립니다.

### 7-3. ALB 요청 분산 확인

ALB의 Details에서 DNS name을 복사합니다.

**작업 화면: 로컬 터미널**

```bash
# 실제 ALB DNS 이름을 현재 터미널 변수에 저장한다.
ALB_DNS='<week8-alb의 실제 DNS 이름>'

# ALB가 HTTP 200을 반환하는지 확인한다.
curl -i "http://$ALB_DNS/server-id.txt"

# 같은 주소를 여러 번 호출해 어느 EC2가 응답하는지 확인한다.
i=1
while [ "$i" -le 10 ]; do
  curl -s "http://$ALB_DNS/server-id.txt?request=$i"
  i=$((i + 1))
done
```

### 명령 해설

| 명령 | 역할 |
| --- | --- |
| `ALB_DNS='...'` | 긴 ALB 주소를 현재 터미널 변수에 저장한다. |
| `curl -i` | 응답 헤더와 본문을 함께 출력해 HTTP 상태를 본다. |
| `curl -s` | 진행률은 숨기고 응답 본문만 출력한다. |
| `while ...` | 같은 요청을 10번 반복한다. |
| `?request=$i` | 요청 URL을 서로 다르게 만들어 브라우저 캐시와 구분하기 쉽게 한다. |

`week8-web-1`, `week8-web-2`가 섞여 나오면 정상입니다. ALB는 반드시 1, 2, 1, 2 순서로 번갈아 보내는 것이 아니므로 두 결과의 개수가 정확히 같을 필요는 없습니다.

이제 브라우저에서 `http://<ALB_DNS>/`를 열고 WordPress의 사이트 제목과 관리자 계정을 입력해 초기 설정을 한 번만 완료합니다. WordPress 관리자 비밀번호는 문서나 GitHub에 기록하지 않습니다.

설정이 끝난 뒤 같은 ALB 주소를 새로 고쳐 같은 사이트가 계속 보이는지 확인합니다. 두 EC2는 같은 RDS의 WordPress 테이블을 사용하므로 어느 서버가 응답해도 사이트 데이터가 같아야 합니다.

### 7-4. EC2 직접 HTTP 접근 닫기

ALB가 정상인 것을 확인한 뒤 `week8-web-sg`에서 **현재 공인 IP `/32` 출처의 HTTP 80 임시 규칙만 삭제**합니다. `week8-alb-sg` 출처의 HTTP 80과 현재 IP 출처의 SSH 22는 유지합니다.

확인 결과는 다음과 같아야 합니다.

| 주소 | 결과 |
| --- | --- |
| `http://<ALB_DNS>/` | 접속 성공 |
| `http://<WEB1_PUBLIC_IP>/` | 직접 HTTP 접속 실패 |
| `http://<WEB2_PUBLIC_IP>/` | 직접 HTTP 접속 실패 |
| EC2 SSH | 현재 공인 IP에서 계속 성공 |

EC2가 Public Subnet에 있어도 Security Group이 ALB의 HTTP만 허용하므로 사용자의 웹 요청은 ALB를 거쳐야 합니다.

---

## 8. 웹 서버 한 대 장애와 ALB 우회 확인

**이 단계에서 확인하는 것:** 한 EC2의 Apache가 멈추면 ALB가 해당 Target을 제외하고 나머지 서버로 응답하는지 확인합니다.

### 8-1. web-1의 Apache 중지

**작업 화면: `week8-web-1` SSH 터미널**

```bash
# EC2 자체는 유지하고 웹 서버 프로세스만 중지한다.
sudo systemctl stop httpd

# 중지 상태인지 확인한다.
sudo systemctl is-active httpd
```

`inactive`가 출력됩니다.

### 8-2. Target 상태와 사용자 응답 확인

Target Group의 Targets 탭에서 `week8-web-1`이 `Unhealthy`, `week8-web-2`가 `Healthy`가 될 때까지 기다립니다. Health Check 간격과 실패 기준 때문에 즉시 바뀌지 않을 수 있습니다.

**작업 화면: 로컬 터미널**

```bash
# 장애 중에도 ALB가 정상 서버의 200 응답을 반환하는지 10번 확인한다.
i=1
while [ "$i" -le 10 ]; do
  curl -sS -o /dev/null -w 'HTTP %{http_code}\n' "http://$ALB_DNS/?failure-test=$i"
  i=$((i + 1))
done

# 실제 응답 서버가 web-2뿐인지 확인한다.
i=1
while [ "$i" -le 5 ]; do
  curl -s "http://$ALB_DNS/server-id.txt?failure-test=$i"
  i=$((i + 1))
done
```

첫 명령은 `HTTP 200`, 두 번째 명령은 `week8-web-2`만 출력되어야 합니다.

### 8-3. web-1 복구

**작업 화면: `week8-web-1` SSH 터미널**

```bash
# Apache를 다시 시작한다.
sudo systemctl start httpd

# 프로세스와 로컬 HTTP 응답을 확인한다.
sudo systemctl is-active httpd
curl -i http://localhost/server-id.txt
```

Target Group에서 web-1이 다시 `Healthy`가 된 뒤 반복 요청에 두 서버가 모두 나오는지 확인합니다.

ALB는 장애를 자동으로 수리한 것이 아닙니다. Health Check 실패 서버를 요청 대상에서 빼고, Apache 복구 후 다시 포함한 것입니다.

---

## 9. CloudWatch에서 요청·장애·DB 상태 관찰하기

**이 단계에서 확인하는 것:** 지표로 요청 증가와 장애 발생 시점을 찾고, 각 지표가 어느 자원의 상태를 나타내는지 구분합니다.

### 9-1. 관찰용 요청 생성

**작업 화면: 로컬 터미널**

```bash
# CloudWatch 그래프에 확인할 요청이 생기도록 ALB에 100건을 순차 전송한다.
i=1
while [ "$i" -le 100 ]; do
  curl -sS -o /dev/null "http://$ALB_DNS/server-id.txt?cloudwatch=$i"
  i=$((i + 1))
done
```

이 명령은 ALB까지 요청을 보낸 뒤 Healthy EC2의 응답을 받을 때까지 100번 반복합니다. 응답 본문은 버리지만 ALB의 `RequestCount`와 EC2의 네트워크 지표에는 요청이 반영됩니다.

### 9-2. CloudWatch 지표 확인

**작업 화면: CloudWatch → Metrics → All metrics**

다음 지표를 같은 시간 범위로 확인합니다. 기본 지표는 수집 주기 때문에 요청 직후 바로 보이지 않을 수 있으므로 몇 분 기다립니다.

| Namespace·대상 | 지표 | 확인할 내용 |
| --- | --- | --- |
| ApplicationELB · LoadBalancer | `RequestCount` | 100건 요청을 보낸 시점에 값 증가 |
| ApplicationELB · TargetGroup | `HealthyHostCount` | 정상 시 2, 장애 시험 중 1 |
| ApplicationELB · TargetGroup | `UnHealthyHostCount` | web-1 중지 구간에 증가 |
| ApplicationELB · LoadBalancer | `TargetResponseTime` | 대상 서버가 응답하는 데 걸린 시간 |
| EC2 · Per-Instance Metrics | `CPUUtilization`, `NetworkIn`, `NetworkOut` | 두 EC2의 부하와 통신량 |
| RDS · Per-Database Metrics | `DatabaseConnections` | WordPress가 연결한 DB 세션 수 |
| RDS · Per-Database Metrics | `CPUUtilization`, `FreeableMemory` | DB CPU와 남은 메모리 |
| RDS · Per-Database Metrics | `ReadLatency`, `WriteLatency` | DB 읽기·쓰기 지연 |

### 9-3. 대시보드와 알람 만들기

**작업 화면: CloudWatch → Dashboards → Create dashboard**

`week8-dashboard`를 만들고 다음 위젯을 추가합니다.

- ALB `RequestCount`
- Target Group `HealthyHostCount`, `UnHealthyHostCount`
- RDS `DatabaseConnections`, `CPUUtilization`
- 두 EC2의 `CPUUtilization`

**작업 화면: CloudWatch → Alarms → Create alarm**

`UnHealthyHostCount >= 1`을 기준으로 `week8-unhealthy-target` 알람을 만듭니다. 이번 실습에서는 알림 동작을 연결하지 않아도 됩니다. 8단계처럼 Apache를 중지했을 때 `OK → In alarm`, 복구하면 다시 `OK`로 바뀌는지 확인합니다.

CloudWatch의 EC2 기본 지표만으로는 Apache 요청 한 건의 상세 URL이나 오류 메시지를 알 수 없습니다. 그런 로그까지 중앙 수집하려면 CloudWatch Agent나 별도의 로그 수집 구성이 필요합니다. 이번 실습은 AWS 기본 지표와 Lambda 기본 로그 범위로 제한합니다.

### 9-4. 선택: 기존 k6로 ALB에 부하 보내기

6주차에 사용한 k6가 로컬에 남아 있다면 ALB 주소를 대상으로 실행해 `RequestCount`, `TargetResponseTime`, EC2 CPU 변화를 더 크게 관찰할 수 있습니다. 새 부하 도구를 설치하는 것은 필수가 아닙니다.

---

## 10. API Gateway와 Lambda로 서버리스 요청 비교하기

**이 단계에서 확인하는 것:** EC2를 새로 만들지 않고도 HTTP 주소가 Lambda 함수를 실행하고 CloudWatch Logs를 남기는지 확인합니다.

### 10-1. Lambda 함수 생성

**작업 화면: Lambda → Functions → Create function**

| 항목 | 값 |
| --- | --- |
| Author from scratch | 선택 |
| Function name | `week8-health` |
| Runtime | 콘솔에서 제공하는 최신 Node.js |
| Architecture | x86_64 |
| Execution role | 기본 Lambda 로그 권한을 가진 새 역할 생성 |

기본 코드를 다음 코드로 교체하고 **Deploy**를 누릅니다.

```javascript
export const handler = async (event, context) => {
  console.log(JSON.stringify({
    event: "health-check",
    requestId: context.awsRequestId,
    path: event.rawPath ?? event.path ?? "/"
  }));

  return {
    statusCode: 200,
    headers: { "content-type": "application/json" },
    body: JSON.stringify({
      status: "ok",
      service: "week8-health",
      requestId: context.awsRequestId
    })
  };
};
```

Test 탭에서 빈 JSON `{}` 이벤트로 실행합니다. Status가 `Succeeded`, 응답의 `statusCode`가 `200`이면 정상입니다.

### 10-2. API Gateway HTTP API 연결

**작업 화면: API Gateway → Create API → HTTP API → Build**

1. Integration에 `week8-health` Lambda를 선택합니다.
2. API name을 `week8-http-api`로 정합니다.
3. Route를 `GET /health`로 만듭니다.
4. Stage는 자동 배포되는 `$default`를 사용합니다.
5. 생성 후 Invoke URL을 기록합니다.

### 10-3. HTTP 응답과 Lambda 로그 확인

**작업 화면: 로컬 터미널**

```bash
# 실제 API Gateway Invoke URL을 저장한다. 주소 끝의 슬래시는 중복하지 않는다.
API_URL='<실제 Invoke URL>'

# API Gateway가 Lambda를 호출해 200 JSON을 반환하는지 확인한다.
curl -i "$API_URL/health"
```

`HTTP 200`, `"status":"ok"`, `"service":"week8-health"`가 보이면 정상입니다.

**작업 화면: CloudWatch → Log groups → `/aws/lambda/week8-health`**

가장 최근 Log stream에서 `health-check`와 같은 `requestId`를 확인합니다.

| 확인 위치 | 정상 결과 |
| --- | --- |
| API Gateway | `GET /health` route가 Lambda와 연결됨 |
| curl | HTTP 200과 JSON 본문 |
| Lambda Monitor | `Invocations` 증가, `Errors` 0 |
| CloudWatch Logs | 실행 시간과 `health-check` 로그 |

API Gateway는 외부 HTTP 입구, Lambda는 실행 코드, CloudWatch Logs는 실행 기록을 담당합니다. EC2·Apache처럼 계속 실행 중인 웹 서버 프로세스는 이 경로에 없습니다.

### 10-4. 선택 확장

- 현재 구조를 diagrams.net 또는 Mermaid로 그려 Public Subnet, Private DB Subnet, Security Group의 허용 방향을 표시합니다.
- CloudFront를 ALB 앞에 붙이는 경우 WordPress의 동적 페이지는 캐시하지 않고 정적 파일 경로만 캐시해야 합니다. 이 구성은 비용과 캐시 무효화가 추가되므로 필수 실습에는 넣지 않습니다.

---

## 11. 유료 자원 정리

**이 단계에서 확인하는 것:** 실습용 서비스가 종료된 뒤에도 과금 자원이 남지 않게 의존 관계 순서대로 삭제합니다.

> 아래 자원에 보존해야 할 데이터가 없는 실습 계정인지 먼저 확인합니다. 실습 데이터를 남겨야 한다면 삭제 전에 필요한 백업을 별도로 결정합니다.

### 삭제 순서

1. **API Gateway**: `week8-http-api` 삭제
2. **Lambda**: `week8-health` 삭제
3. **IAM Role**: Lambda 생성 때 만든 역할이 다른 곳에서 사용되지 않으면 삭제
4. **CloudWatch**: `week8-dashboard`, `week8-unhealthy-target` 삭제
5. **CloudWatch Logs**: `/aws/lambda/week8-health`가 필요 없으면 삭제
6. **ALB**: `week8-alb` 삭제 후 완전히 사라질 때까지 대기
7. **Target Group**: `week8-web-tg` 삭제
8. **EC2**: `week8-web-1`, `week8-web-2` 종료가 아니라 **Terminate**
9. **AMI**: `week8-web-ami` 등록 해제(Deregister)
10. **Snapshot**: AMI가 만든 EBS Snapshot 삭제
11. **RDS**: `week8-mysql` 삭제. 폐기 가능한 실습 DB라면 최종 스냅샷을 만들지 않고 자동 백업 보존도 끔
12. **수동 RDS Snapshot**: 남아 있으면 삭제
13. **Security Group**: `week8-alb-sg`, `week8-web-sg`, `week8-db-sg` 삭제
14. **Subnet·Route Table·Internet Gateway**: 연결을 해제하며 삭제
15. **VPC**: `week8-vpc` 삭제
16. **EC2 Key Pair**: 더 쓰지 않으면 AWS 콘솔에서 삭제하고 로컬 `.pem`도 안전하게 정리

RDS와 ALB 삭제는 몇 분 걸릴 수 있습니다. 연결된 자원이 남아 있으면 VPC나 Security Group 삭제가 실패하므로 위 순서를 유지합니다.

### 최종 확인

AWS 콘솔에서 `week8` 이름과 `Project=server-study`, `Week=8` 태그를 검색해 다음 상태를 기록합니다.

| 자원 | 완료 기준 |
| --- | --- |
| EC2 | 실행 중인 week8 인스턴스 없음 |
| EBS·Snapshot·AMI | 불필요한 week8 항목 없음 |
| RDS | week8 DB와 수동 Snapshot 없음 |
| Load Balancer·Target Group | week8 항목 없음 |
| Lambda·API Gateway | week8 항목 없음 |
| CloudWatch | 실습용 Dashboard·Alarm·불필요한 Log Group 없음 |
| VPC | week8-vpc 삭제 완료 |

## 전체 성공 기준

- [ ] Public Subnet과 Private DB Subnet의 Route Table 차이를 설명할 수 있다.
- [ ] ALB만 HTTP 공개되고 EC2 HTTP는 ALB SG에서만 허용된다.
- [ ] RDS는 Public access가 `No`이고 DB SG가 web SG의 `3306`만 허용한다.
- [ ] 두 EC2가 같은 RDS의 WordPress 데이터를 보여 준다.
- [ ] ALB 반복 요청에서 두 EC2의 식별 값이 확인된다.
- [ ] Apache 한 대 중지 후에도 ALB가 `200`을 반환한다.
- [ ] CloudWatch에서 요청 수, 정상 대상 수, DB 연결 수를 찾았다.
- [ ] API Gateway `GET /health`가 Lambda의 `200` JSON과 로그를 만든다.
- [ ] 실습용 유료 자원을 모두 삭제했다.

## 공식 참고 자료

- [Amazon Linux 2023 WordPress 설치](https://docs.aws.amazon.com/linux/al2023/ug/hosting-wordpress-aml-2023.html)
- [RDS DB 인스턴스 생성](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_CreateDBInstance.html)
- [ALB 생성과 테스트](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/create-application-load-balancer.html)
- [ALB Health Check 설정](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/target-group-health-checks.html)
- [CloudWatch 알람](https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch_Alarms.html)
- [API Gateway HTTP API 시작하기](https://docs.aws.amazon.com/apigateway/latest/developerguide/getting-started.html)
